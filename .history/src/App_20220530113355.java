/**JAVA文件的编写必须有一个类，并且类名必须与文件名相同，在这个类中必须有一个main函数，
这样当java程序执行是，会先找到文件名的类，执行里面的main函数，
public class App {
    public static void main(String[] args) throws Exception {
        System.out.println("Hello, World!");
    }
}
